import routes from '../navigation/routes'

const menu = {
  home: {
    route: routes.home,
    labelId: 'navigation.home',
  },
  hello: {
    route: routes.hello,
    labelId: 'navigation.hello',
  },
  goodbye: {
    route: routes.goodbye,
    labelId: 'navigation.goodbye',
  },
}

export default menu
